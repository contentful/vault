package test;

import com.contentful.sqliteprocessor.SqliteHelper;

public class Test$QLite implements SqliteHelper {
  static final String NAME = "entry_y2lk";

  static final String CREATE = "CREATE TABLE `" + NAME + "` ("
      + "`_ID` INTEGER PRIMARY KEY AUTOINCREMENT,"
      + "`remote_id` STRING NOT NULL,"
      + "`fBoolean` INT,"
      + "`fInteger` INT,"
      + "`fDouble` DOUBLE,"
      + "`fText` STRING,"
      + "`fMap` BLOB"
      + ");";

  @Override public String getTableName() {
    return NAME;
  }

  @Override public String getCreateStatement() {
    return CREATE;
  }
}